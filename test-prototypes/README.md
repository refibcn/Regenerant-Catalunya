# Brand Asset Prototypes

This directory contains test outputs and prototypes generated by the brand asset scripts.

## Directory Structure

```
test-prototypes/
├── input/              # Test input images
├── output/             # Pattern generation outputs
├── templates/          # Generated templates
├── reports/            # Test reports
├── test-scripts.js     # Core functionality tests
├── generate-prototypes.js  # Comprehensive prototype generator
├── TEST-REPORT.md      # Detailed test report
└── README.md           # This file
```

## Quick Start

### Run Tests

```bash
node test-prototypes/test-scripts.js
```

### Generate All Prototypes

```bash
node test-prototypes/generate-prototypes.js
```

## Generated Prototypes

### Pattern Variations

**Halftone Dots:**

- `prototype-dots-{4,8,12,16}px.png` - Different dot sizes
- `prototype-intensity-{0.3,0.5,0.7,0.9}.png` - Different intensities
- `prototype-angle-{0,45,90,135}deg.png` - Different angles

**Halftone Lines:**

- `prototype-lines-{2,4,6,8}px.png` - Different line spacing

**Complete Workflow:**

- `prototype-complete-final.png` - Pattern + duotone example

### Templates

**Social Media:**

- `{instagram,twitter,linkedin,facebook}-{en,es,cat}.png`
- `{platform}-template.svg` - Editable SVG templates

**Web Banners:**

- `banner-{hero,section,sidebar,mobile}.png`
- `banner-{type}.webp` - Optimized WebP
- `banner-{type}.svg` - Scalable SVG
- `banner-{type}.html` - Quartz HTML snippets

## Test Results

✅ **All 6 core tests passing**  
✅ **42 prototype files generated**  
✅ **All scripts functional**

See `TEST-REPORT.md` for detailed results.

## Usage Examples

### Process Event Photo

```bash
# Step 1: Apply halftone pattern
node scripts/brand-assets/pattern-generation/halftone-dots.js \
  --input path/to/photo.jpg \
  --output path/to/patterned.png \
  --config brand-assets/pattern-config.json

# Step 2: Apply duotone
node scripts/brand-assets/image-processing/apply-duotone.js \
  --input path/to/patterned.png \
  --output path/to/final.png \
  --config brand-assets/pattern-config.json
```

### Generate Social Media Post

```bash
node scripts/brand-assets/template-generation/social-media.js \
  --platform instagram \
  --output-dir templates/social-media \
  --languages en,es,cat
```

### Generate Web Banner

```bash
node scripts/brand-assets/template-generation/web-banners.js \
  --type hero \
  --output-dir templates/banners \
  --quartz-integration true
```

## Next Steps

1. Review generated prototypes
2. Adjust pattern settings if needed
3. Integrate into production workflows
4. Use templates for actual content creation

---

**Generated:** November 13, 2025  
**Status:** ✅ Ready for Review
